apiVersion: v1
kind: Service
metadata:
  name: qwen-model-service
  namespace: qwen
  labels:
    app: qwen-model
    component: inference
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: fastapi
  selector:
    app: qwen-model
