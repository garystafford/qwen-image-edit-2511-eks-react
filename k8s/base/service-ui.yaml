apiVersion: v1
kind: Service
metadata:
  name: qwen-ui-service
  namespace: qwen
  labels:
    app: qwen-ui
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: qwen-ui
